# Copy to .env alongside each host's docker-compose.yml (.env is gitignored).
#
#   scp network/pihole/.env.example <host>:<compose-dir>/.env   # then fill it in
#
# Every host uses the same value today; nothing requires that to stay true.
PIHOLE_WEBSERVER_API_PASSWORD=changeme
