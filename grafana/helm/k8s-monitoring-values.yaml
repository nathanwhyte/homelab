# Kubernetes Monitoring Helm Chart Values
# Chart: grafana/k8s-monitoring
# Purpose: Deploy Alloy for log collection, forwarding to existing Loki

# Cluster configuration
cluster:
  name: k8s

# Destinations - logs to Loki, metrics to Prometheus (remote write)
# Enables Alloy to gather logs and metrics and deliver to the stack.
destinations:
  - name: loki
    type: loki
    # Point to existing Loki gateway service
    url: http://loki-gateway.grafana.svc.cluster.local/loki/api/v1/push
  - name: prometheus
    type: prometheus
    # kube-prometheus-stack Prometheus remote write receiver (fullnameOverride: prom)
    url: http://prom-prometheus.grafana.svc.cluster.local:9090/api/v1/write

# Cluster events collection
clusterEvents:
  enabled: false
  collector: alloy-singleton

# Node logs
nodeLogs:
  enabled: true
  # Send to Loki (explicit so logs flow to the provisioned Loki datasource)
  destinations:
    - loki
  # Collector is automatically set to alloy-logs

# Pod logs collection
podLogs:
  enabled: true
  gatherMethod: kubernetesApi
  destinations:
    - loki

# Collectors
alloy-singleton:
  enabled: true
  alloy:
    # Resource limits (increased for better performance)
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi

# Enable Alloy to scrape cluster metrics and remote-write to Prometheus
alloy-metrics:
  enabled: true
  alloy:
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi

alloy-logs:
  enabled: true
  alloy:
    mounts:
      varlog: true
      dockercontainers: false
    clustering:
      enabled: true
    # Resource limits (increased for better performance)
    resources:
      requests:
        cpu: 200m
        memory: 256Mi
      limits:
        cpu: 1000m
        memory: 1Gi

alloy-profiles:
  enabled: false

alloy-receiver:
  enabled: false

# Alloy Operator - manages Alloy instances
alloy-operator:
  # Deploy the Alloy Operator (manages Alloy instances)
  deploy: true
