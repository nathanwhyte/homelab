---
apiVersion: v1
kind: Service
metadata:
  name: llama-api
  namespace: llama
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8000
  selector:
    app: llamacpp-openai
  sessionAffinity: None
