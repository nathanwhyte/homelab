# The four reference files under `reference/` are also formatter-skipped — they're
# navigation indexes, prettier would re-pad their already-compact tables. Keep them
# hand-authored so the link targets stay short.
# Reference-table file convention (added 2026-06-30 when CLAUDE.md was slimmed to fit the
# 40k SessionStart cap): tables the agent must `Read` on demand live in `reference/*.md`,
# CLAUDE.md keeps only the short orienting tables (topology, ROCm guardrails, middlewares,
# secrets) that should be in front of the agent at all times.
reference/
