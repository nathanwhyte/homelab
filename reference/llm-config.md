# LLM configuration reference

Exact tuning (ctx-size, batch/ubatch, KV cache, resources, env) lives in the manifests —
single source of truth. This file is the navigation index. Lives in `reference/` (not
`CLAUDE.md`) so the SessionStart hook stays under the 40k char cap.

| Service              | Node / GPU          | Model                                                 | Slots | Role                                                                                                                                                                                                                                                                                                                                                                             | Manifest                                              |
| -------------------- | ------------------- | ----------------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| llamacpp-cuda-ov     | manu / GTX 1080     | Qwen3-8B IQ4_XS, ctx=32768                            | 4     | **VLM failover** (`vlm.backup`) since 2026-07-05 — primary is cloud via `chat-ollama.llama.svc:11434/v1` (IDEA-1050; `gemma4:31b-cloud`, non-thinking, settled 2026-07-06 — `qwen3.5:cloud` + `reasoning_effort` crashlooped OV v0.4.7, which doesn't recognize that config field). Kept at replicas=1 for the soak window; retirement candidate (frees the 1080). ~29-30 tok/s gen, ~414 tok/s prompt eval.                                                                                                                        | `viking/manifests/cuda-llamacpp-deployment.yaml`      |
| embedder-qwen        | timmy / RX 9070 XT  | Qwen3-Embedding-4B Q8_0, ctx=8192                     | 1     | **Rollback (ROCm)** since the 2026-07-17 cutover (IMPR-1077) — retained `replicas=1` through the soak, then scaled to 0; **the `embedder-qwen` Service no longer selects this Deployment** (selector moved to `app=embedder-qwen-cuda`). Was primary 2026-07-06→07-17. 2560-dim, `--pooling last`, `--n-gpu-layers 999`, `--flash-attn on`, `--batch-size 4096 --ubatch-size 4096` (9070 XT has headroom), **no `--mlock`** (timmy is control-plane). ROCm gfx1201 (`privileged: true`, `HIP_VISIBLE_DEVICES=0`, mounts `/opt/rocm-7.2.1/lib/rocblas/library`). Model on `embedder-qwen-model-cache` PVC. Deployment renamed backend-neutral (IMPR-1040). Roll back: repoint the `embedder-qwen` Service selector to `app=embedder-qwen` (scale back to 1 first if already at 0). | `viking/manifests/embedder-qwen-rocm-deployment.yaml` |
| embedder-qwen-cuda   | manu / GTX 1080     | Qwen3-Embedding-4B Q8_0, ctx=8192                     | 1     | **Primary embedder** since the 2026-07-17 cutover (IMPR-1077; relocated from wemby/1060 → manu/1080, TASK-1136 throughput decision). Same model/quant/pooling as the ROCm pod → identical 2560-dim vectors, gated by a min-cosine 0.9995 equivalence check (measured 0.99954) so no re-embed was needed. 2560-dim, `--pooling last`, `--ctx-size 8192 --parallel 1 --batch-size 512 --ubatch-size 512`, `--n-gpu-layers 999`, `--flash-attn on` (~5.7 GB VRAM). CUDA `server-cuda` (`runtimeClassName: nvidia`, `nvidia.com/gpu: 1`). Own `embedder-cuda-model-cache` PVC (coexists with the ROCm pod's cache during the transition). The neutral `embedder-qwen` Service selects `app=embedder-qwen-cuda` (cosmetic deviation from IMPR-1040). | `viking/manifests/embedder-qwen-cuda-deployment.yaml` |
| embedder-llamacpp    | wemby / GTX 1060    | nomic-embed-text-v1.5 f16, ctx=16384                  | 2     | **Deleted 2026-07-04** — Deployment and Service removed from the cluster (retired 2026-06-30 at replicas=0 first). Manifest retained in repo for history only; resurrecting it would require a vectordb wipe + re-embed + OV config dim change (2560→768).                                                                                                                       | `viking/manifests/embedder-llamacpp-deployment.yaml`  |
| ollama               | timmy / RX 9070 XT  | deepseek-coder-v2:fim (16B-lite MoE Q4, ~13 GB, single resident since 2026-07-24); gemma4:31b-cloud (OV VLM, cloud tag, no local slot) | 2     | **Edit-prediction node** (IMPR-1083, 2026-07-19; model cutover 2026-07-24) — Zed (`prompt_format "deepseek_coder"`), Minuet (suffix FIM via the library template), and remote VSCode FIM all route to the one deepseek tag. zeta2.1 + qwen2.5-coder:fim retired from the warm list (BUG-1037: open-weights Zeta degenerates below ~3k prompt tokens vs Zed's hardcoded ~500-token self-hosted excerpts; deepseek benchmarked P50 TTFT 0.59s / ~167 tok/s vs qwen2.5-coder:14b-base 1.33s / ~57 tok/s, identical FIM smoke output; old tags kept on the PVC for rollback). gemma4:12b-it-qat orphaned and no longer warmed (Hermes/mem0 retired). **Vulkan backend** (`OLLAMA_VULKAN=1`, `OLLAMA_LLM_LIBRARY=vulkan`, `GGML_VK_VISIBLE_DEVICES=1` selects the discrete card — beats ROCm ~10-20% agg tok/s, INFO-1084); LoadBalancer; `OLLAMA_MAX_LOADED_MODELS=1` (dropped from 2 at the cutover — one pinned runner, no co-load; ad-hoc loads swap explicitly); `OLLAMA_NUM_PARALLEL=2` (edit-prediction is low-concurrency, ~1-2 in-flight per editor), `OLLAMA_CONTEXT_LENGTH=131072` (TASK-1188/IMPR-1114; KV allocated on demand — see the manifest comment), `OLLAMA_KV_CACHE_TYPE=q8_0`, `OLLAMA_KEEP_ALIVE=-1` (pinned), `CAP_PERFMON` (accurate free-VRAM packing on the de-privileged pod); chat proxy suppresses reasoning for local models.                                                                                                                                | `llama/ollama-deployment.yaml`                        |
| hermes-agent         | — (no nodeSelector) | glm-5.1:cloud (primary), gemma4:12b-it-qat (fallback) | 1     | `registry.nathanwhyte.dev/homelab/hermes-agent-mem0:fe57dd3` (mem0 adapter sidecar; imagePullPolicy: IfNotPresent, immutable SHA tags); s6-overlay supervises gateway + dashboard; SSH via hermes-jump (PVC-backed in manifest, not yet applied live); `hermes/operator.sh` CLI; **mem0 memory provider** (adapter on localhost:18080); OV KB tools via `OPENVIKING_ENDPOINT`; `args: ["gateway", "run"]`. | `hermes/hermes-deployment.yaml`                       |
| llama-server (local) | MacBook M5 Max      | Qwen3.6-27B-uncensored-heretic-v2 Q4_K_M + mmproj     | 1     | VLM (local, on-demand); `img-pipeline.sh up/down`; `/no_think` suffix for direct responses; fallback for Ollama vision.                                                                                                                                                                                                                                                          | `~/code/robots/media/pipeline/img-pipeline.conf`      |
| Ollama FLUX          | MacBook M5 Max      | FLUX.2 Klein 9B (non-commercial)                      | 1     | Image gen; persistent Ollama; `img-pipeline.sh generate`; 4B via `--model x/flux2-klein`.                                                                                                                                                                                                                                                                                        | `~/code/robots/media/pipeline/img-pipeline.conf`      |

All current production llamacpp services set `--flash-attn on` and use `Strategy: Recreate`. The VLM (`cuda-llamacpp-deployment`) additionally sets `--cont-batching` and KV cache `q4_0` (`--cache-type-k/v`). The primary embedder since the 2026-07-17 cutover (IMPR-1077) is Deployment `embedder-qwen-cuda` (`embedder-qwen-cuda-deployment.yaml`, manu GTX 1080, CUDA) — `--batch-size/--ubatch-size 512`, own `embedder-cuda-model-cache` PVC; the neutral `embedder-qwen` Service selects it. The ROCm `embedder-qwen` Deployment (timmy 9070 XT, `--batch-size/--ubatch-size 4096`, drops `--mlock`) is retained as `replicas=1` rollback through the soak, then scaled to 0. `embedder-llamacpp` was deleted from the cluster on 2026-07-04; `embedder-qwen` (the Service) remains the sole live embedder endpoint.
