---
apiVersion: v1
kind: Service
metadata:
  name: llamacpp-cuda-llm
  namespace: viking
spec:
  type: ClusterIP
  selector:
    app: llamacpp-cuda-ov
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
