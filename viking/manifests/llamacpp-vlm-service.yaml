---
apiVersion: v1
kind: Service
metadata:
  name: llamacpp-vlm
  namespace: viking
spec:
  type: ClusterIP
  selector:
    vlm-pool: "true"
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
