# homework-robot — Qwen3.6-27B-uncensored-heretic-v2 for text use
#
# Rebuilds the homework-robot model with:
#   - Proper Qwen3 chat template (role formatting, tool call support)
#   - Thinking DISABLED by default via /no_think in the template suffix
#     (Ollama auto-enables "thinking" capability for Qwen3.6 GGUF models,
#     causing the model to get "stuck" generating long reasoning chains before
#     responding. The /no_think suffix suppresses this at the model level.)
#   - To re-enable thinking per-request, pass think:true in the API call
#
# NOTE: This model cannot do vision through Ollama (split mmproj import is
# unsupported — see TASK-016). For image understanding, use llama-server
# via img-pipeline.sh understand, which loads the mmproj separately.
#
# Build:  ollama create homework-robot -f scripts/homework-robot.Modelfile
# Run:    ollama run homework-robot "your prompt here"
#
FROM /Users/noot/code/robots/media/homework/models/Qwen3.6-27B-uncensored-heretic-v2-Native-MTP-Preserved-Q4_K_M.gguf

TEMPLATE """{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{- end }}
{{- range .Messages }}<|im_start|>{{ .Role }}
{{ .Content }}{{- if eq .Role "assistant" }}<|im_end|>
{{- end }}
{{- end }}<|im_start|>assistant
/no_think"""

SYSTEM """You are a helpful, direct assistant. Provide clear and thorough answers. When doing math or reasoning, show your work step by step. Do not refuse reasonable requests."""

PARAMETER num_ctx 32768
PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER stop "<|im_start|>"
PARAMETER stop "<|im_end|>"